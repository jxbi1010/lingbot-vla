# Extends ego_finetune_v3.yaml. Put cluster-only train overrides here (e.g. output_dir,
# wandb_name, micro_batch_size, global_batch_size, lr, eval_frequency); model/data and
# other train fields come from the base file. Merge: nested `train` is merged over base.
_base_: ego_finetune_v3.yaml

data:
  dataset_init_main_process_first: false

train:
  output_dir: /home/ss-oss1/checkpoints/jianxin/output/ego_21w_v3_batch256_cluster
  wandb_name: ego-21w-256-multinodes
  micro_batch_size: 32
  global_batch_size: 256
  lr: 1.0e-4
  eval_frequency: 100
  eval_steps: 2
